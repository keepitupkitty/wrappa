1. Make this daemon to check if the caller user is "wheel" (and not other groups because of socket permission)
2. Add anti-ddos protection
3. apply highly restriction seccomp filters (syscalls + permit ONLY AF_INET and AF_INET6 and AF_UNIX)
4. apply landlock/unveil policy for wrappad
5. sandbox filesystem in child
6. limit wrappa to /usr/bin/{su,sudo,doas} only (fd = open("/usr/bin", O_RDONLY), execveat(fd, "su", ...))
7. add config file support and make 0600 (enforce by daemon)
